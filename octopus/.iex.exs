alias Octopus.{
  Protobuf,
  AppSupervisor,
  AppRegistry,
  Mixer,
  ColorPalette,
  Apps,
  Font,
  Broadcaster,
  Transitions
}

IEx.configure(inspect: [limit: :infinity])
Logger.configure(level: :info)
